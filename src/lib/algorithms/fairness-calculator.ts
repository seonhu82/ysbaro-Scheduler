// 형평성 계산 ⭐⭐

export function calculateFairnessScore(
  nightShifts: number,
  weekendShifts: number,
  allStaffMetrics: { nightShifts: number; weekendShifts: number }[]
) {
  // TODO: 형평성 점수 계산
  // 1. 평균 계산
  // 2. 편차 계산
  // 3. 가중 평균 (야간 3, 주말 2)
  // 4. 등급 산정

  return {
    score: 0,
    grade: 'GOOD',
  }
}
