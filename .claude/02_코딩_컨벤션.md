# 연세바로치과 스케줄러 - 코딩 컨벤션

**일관된 코드 작성을 위한 규칙**
**작성일**: 2025-10-22
**버전**: 1.0

---

## 📝 목차

1. [TypeScript 규칙](#typescript-규칙)
2. [네이밍 컨벤션](#네이밍-컨벤션)
3. [파일 구조](#파일-구조)
4. [React 컴포넌트](#react-컴포넌트)
5. [API 라우트](#api-라우트)
6. [데이터베이스](#데이터베이스)
7. [에러 처리](#에러-처리)
8. [주석 및 문서화](#주석-및-문서화)
9. [Git 커밋](#git-커밋)

---

## TypeScript 규칙

### 타입 정의

✅ **권장**:
```typescript
// 인터페이스 사용 (확장 가능)
interface User {
  id: string
  email: string
  name: string
}

// 타입 별칭 (유니온, 유틸리티 타입)
type UserRole = 'ADMIN' | 'VIEWER'
type OptionalUser = Partial<User>
```

❌ **비권장**:
```typescript
// any 사용 금지
const data: any = fetchData()  // ❌

// 올바른 방법
const data: User = fetchData()  // ✅
```

### 타입 임포트

```typescript
// 타입만 임포트할 때는 명시
import type { User } from '@/types'
import { getUser } from '@/lib/api'  // 값 임포트
```

### null과 undefined

```typescript
// Optional Chaining 사용
const userName = user?.profile?.name

// Nullish Coalescing 사용
const displayName = userName ?? 'Guest'

// null 체크
if (user === null) {  // undefined가 아닌 null 명시
  // ...
}
```

---

## 네이밍 컨벤션

### 파일명

```
컴포넌트: PascalCase
  ✅ CalendarView.tsx
  ✅ NotificationBell.tsx

유틸리티/서비스: kebab-case
  ✅ leave-service.ts
  ✅ date-utils.ts

API 라우트: kebab-case
  ✅ route.ts (폴더명이 kebab-case)
  ✅ /api/leave-apply/[token]/route.ts

타입: kebab-case
  ✅ leave.ts
  ✅ notification.ts
```

### 변수명

```typescript
// 상수: UPPER_SNAKE_CASE
const MAX_SLOTS_PER_DAY = 3
const API_BASE_URL = 'https://api.example.com'

// 변수: camelCase
const userName = 'John'
const isActive = true
const staffList = []

// Boolean: is/has 접두사
const isLoading = false
const hasPermission = true
const canEdit = false

// 함수: camelCase + 동사
function fetchUserData() { }
function calculateTotal() { }
function validateInput() { }
```

### 컴포넌트명

```typescript
// 컴포넌트: PascalCase
function UserProfile() { }
function LeaveManagementDashboard() { }

// 커스텀 훅: use 접두사
function useSchedule() { }
function useLeaveManagement() { }
function useNotifications() { }
```

### API 함수명

```typescript
// CRUD: get/create/update/delete
async function getSchedule(id: string) { }
async function createLeaveApplication(data: any) { }
async function updateStaff(id: string, data: any) { }
async function deleteSchedule(id: string) { }

// 조회: find/fetch
async function findStaffByRank(rank: string) { }
async function fetchSchedules(year: number, month: number) { }

// 검증: validate/check
function validateSlotLimit(count: number) { }
function checkWeeklyOffLimit(staffId: string) { }
```

---

## 파일 구조

### 컴포넌트 파일

```typescript
// src/components/calendar/CalendarView.tsx

'use client'  // 클라이언트 컴포넌트만

import { useState, useEffect } from 'react'  // React 먼저
import { useRouter } from 'next/navigation'  // Next.js
import { format } from 'date-fns'  // 외부 라이브러리

import { Button } from '@/components/ui/button'  // 내부 컴포넌트
import { useSchedule } from '@/lib/hooks/use-schedule'  // 훅
import type { Schedule } from '@/types/schedule'  // 타입

// 인터페이스 정의
interface CalendarViewProps {
  year: number
  month: number
  onDateClick?: (date: Date) => void
}

// 메인 컴포넌트
export function CalendarView({ year, month, onDateClick }: CalendarViewProps) {
  // 1. 상태
  const [selectedDate, setSelectedDate] = useState<Date | null>(null)

  // 2. 훅
  const router = useRouter()
  const { schedules, isLoading } = useSchedule(year, month)

  // 3. 핸들러
  const handleDateClick = (date: Date) => {
    setSelectedDate(date)
    onDateClick?.(date)
  }

  // 4. 이펙트
  useEffect(() => {
    // ...
  }, [year, month])

  // 5. 렌더링
  if (isLoading) {
    return <div>Loading...</div>
  }

  return (
    <div>
      {/* JSX */}
    </div>
  )
}

// 서브 컴포넌트 (필요시)
function CalendarCell() {
  return <div />
}
```

### API 라우트 파일

```typescript
// src/app/api/schedule/route.ts

import { NextRequest, NextResponse } from 'next/server'
import { getServerSession } from 'next-auth'

import { prisma } from '@/lib/prisma'
import { authOptions } from '@/lib/auth'
import type { Schedule } from '@/types/schedule'

// GET - 조회
export async function GET(request: NextRequest) {
  try {
    // 1. 인증 확인
    const session = await getServerSession(authOptions)
    if (!session) {
      return NextResponse.json(
        { error: 'Unauthorized' },
        { status: 401 }
      )
    }

    // 2. 파라미터 추출
    const searchParams = request.nextUrl.searchParams
    const year = parseInt(searchParams.get('year') || '')
    const month = parseInt(searchParams.get('month') || '')

    // 3. 검증
    if (!year || !month) {
      return NextResponse.json(
        { error: 'Invalid parameters' },
        { status: 400 }
      )
    }

    // 4. 비즈니스 로직
    const schedules = await prisma.schedule.findMany({
      where: {
        clinicId: session.user.clinicId,
        scheduleDate: {
          gte: new Date(year, month - 1, 1),
          lte: new Date(year, month, 0),
        },
      },
      include: {
        doctors: true,
        staffAssignments: true,
      },
    })

    // 5. 응답
    return NextResponse.json({
      success: true,
      data: schedules,
    })

  } catch (error) {
    console.error('GET /api/schedule error:', error)

    return NextResponse.json(
      { error: 'Internal server error' },
      { status: 500 }
    )
  }
}

// POST - 생성
export async function POST(request: NextRequest) {
  try {
    // 동일한 구조
  } catch (error) {
    // 에러 처리
  }
}
```

### 서비스 파일

```typescript
// src/lib/services/leave-service.ts

import { prisma } from '@/lib/prisma'
import { isSameDay, startOfWeek, endOfWeek } from 'date-fns'
import type { LeaveApplication, Staff } from '@prisma/client'

// 타입 정의
interface SlotAvailability {
  available: boolean
  current: number
  max: number
  remaining: number
}

// 슬롯 확인
export async function checkSlotAvailability(
  linkId: string,
  date: Date
): Promise<SlotAvailability> {
  // 구현
}

// 주간 오프 제한 확인
export async function checkWeeklyOffLimit(
  linkId: string,
  staffId: string,
  date: Date
): Promise<boolean> {
  // 구현
}

// 휴일 확인
export async function isHoliday(
  clinicId: string,
  date: Date
): Promise<boolean> {
  const dayOfWeek = date.getDay()

  // 일요일
  if (dayOfWeek === 0) {
    return true
  }

  // 공휴일
  const holiday = await prisma.holiday.findFirst({
    where: {
      clinicId,
      date: {
        gte: new Date(date.setHours(0, 0, 0, 0)),
        lt: new Date(date.setHours(23, 59, 59, 999)),
      },
    },
  })

  return !!holiday
}

// 연차 신청 생성
export async function createLeaveApplication(
  data: CreateLeaveApplicationData
): Promise<LeaveApplication> {
  // 구현
}
```

---

## React 컴포넌트

### 컴포넌트 패턴

```typescript
// ✅ 권장: 명명된 내보내기
export function CalendarView() {
  return <div />
}

// ❌ 비권장: 기본 내보내기
export default function CalendarView() {  // ❌
  return <div />
}
```

### Props 타입

```typescript
// ✅ 인터페이스 사용
interface ButtonProps {
  children: React.ReactNode
  onClick?: () => void
  variant?: 'primary' | 'secondary'
  disabled?: boolean
}

export function Button({ children, onClick, variant = 'primary', disabled = false }: ButtonProps) {
  return <button />
}
```

### 조건부 렌더링

```typescript
// ✅ 권장: 삼항 연산자 또는 &&
{isLoading ? <Spinner /> : <Content />}
{error && <ErrorMessage />}

// ❌ 비권장: if 문
if (isLoading) {  // ❌
  return <Spinner />
}
return <Content />
```

### 리스트 렌더링

```typescript
// ✅ key 속성 필수
{items.map((item) => (
  <div key={item.id}>  // unique ID 사용
    {item.name}
  </div>
))}

// ❌ 인덱스 key 사용 금지
{items.map((item, index) => (
  <div key={index}>  // ❌
    {item.name}
  </div>
))}
```

### 이벤트 핸들러

```typescript
// ✅ handle 접두사
const handleClick = () => { }
const handleSubmit = (e: FormEvent) => { }
const handleChange = (value: string) => { }

// JSX에서 인라인 화살표 함수 피하기
// ❌ 비권장
<button onClick={() => console.log('clicked')}>Click</button>

// ✅ 권장
<button onClick={handleClick}>Click</button>
```

---

## API 라우트

### 응답 형식

```typescript
// ✅ 성공 응답
return NextResponse.json({
  success: true,
  data: result,
  message: 'Operation completed successfully',  // 선택사항
})

// ✅ 에러 응답
return NextResponse.json(
  {
    success: false,
    error: 'Error message',
    details: { field: 'Invalid value' },  // 선택사항
  },
  { status: 400 }
)
```

### 상태 코드

```typescript
// 200: 성공
return NextResponse.json({ data }, { status: 200 })

// 201: 생성 성공
return NextResponse.json({ data }, { status: 201 })

// 400: 잘못된 요청
return NextResponse.json({ error }, { status: 400 })

// 401: 인증 실패
return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

// 403: 권한 없음
return NextResponse.json({ error: 'Forbidden' }, { status: 403 })

// 404: 찾을 수 없음
return NextResponse.json({ error: 'Not found' }, { status: 404 })

// 409: 충돌 (중복 등)
return NextResponse.json({ error: 'Conflict' }, { status: 409 })

// 500: 서버 에러
return NextResponse.json({ error: 'Internal server error' }, { status: 500 })
```

### 인증 확인

```typescript
// ✅ 모든 보호된 라우트에 인증 확인
export async function GET(request: NextRequest) {
  const session = await getServerSession(authOptions)

  if (!session) {
    return NextResponse.json(
      { error: 'Unauthorized' },
      { status: 401 }
    )
  }

  // ...
}
```

---

## 데이터베이스

### Prisma 쿼리

```typescript
// ✅ include로 관계 로드
const schedule = await prisma.schedule.findUnique({
  where: { id },
  include: {
    doctors: true,
    staffAssignments: {
      include: { staff: true },
    },
  },
})

// ✅ select로 필요한 필드만
const staff = await prisma.staff.findMany({
  select: {
    id: true,
    name: true,
    rank: true,
  },
})

// ✅ 페이지네이션
const applications = await prisma.leaveApplication.findMany({
  skip: (page - 1) * limit,
  take: limit,
  orderBy: { createdAt: 'desc' },
})
```

### 트랜잭션

```typescript
// ✅ 트랜잭션 사용 (일관성 보장)
await prisma.$transaction(async (tx) => {
  // 1. 신청 생성
  const application = await tx.leaveApplication.create({
    data: { /* ... */ },
  })

  // 2. 연차 차감
  await tx.staff.update({
    where: { id: staffId },
    data: {
      annualLeaveUsed: { increment: 1 },
    },
  })

  // 3. 알림 생성
  await tx.notification.create({
    data: { /* ... */ },
  })
})
```

---

## 에러 처리

### try-catch

```typescript
// ✅ API 라우트에서 항상 try-catch
export async function POST(request: NextRequest) {
  try {
    // 비즈니스 로직
  } catch (error) {
    console.error('POST /api/schedule error:', error)

    return NextResponse.json(
      { error: 'Internal server error' },
      { status: 500 }
    )
  }
}
```

### 클라이언트 에러

```typescript
// ✅ React Query 에러 처리
const { data, error, isError } = useQuery({
  queryKey: ['schedules', year, month],
  queryFn: () => fetchSchedules(year, month),
  onError: (error) => {
    toast.error('Failed to fetch schedules')
    console.error(error)
  },
})

if (isError) {
  return <ErrorMessage error={error} />
}
```

### 폼 검증

```typescript
// ✅ Zod 스키마 사용
import { z } from 'zod'

const leaveApplicationSchema = z.object({
  date: z.date(),
  leaveType: z.enum(['ANNUAL', 'OFF']),
  staffId: z.string().uuid(),
})

// 검증
const result = leaveApplicationSchema.safeParse(data)

if (!result.success) {
  return NextResponse.json(
    { error: 'Validation failed', details: result.error.errors },
    { status: 400 }
  )
}
```

---

## 주석 및 문서화

### 함수 주석

```typescript
/**
 * 슬롯 가용성을 확인합니다.
 *
 * @param linkId - 신청 링크 ID
 * @param date - 확인할 날짜
 * @returns 슬롯 가용성 정보
 * @throws {Error} 링크를 찾을 수 없는 경우
 */
export async function checkSlotAvailability(
  linkId: string,
  date: Date
): Promise<SlotAvailability> {
  // 구현
}
```

### 인라인 주석

```typescript
// ✅ 복잡한 로직에만 주석
// 야간 근무와 주말 근무에 가중치를 적용하여 형평성 점수 계산
const score = (nightDev * 3 + weekendDev * 2) / 5

// ❌ 명백한 코드에 불필요한 주석
const total = a + b  // a와 b를 더함 ❌
```

### TODO 주석

```typescript
// TODO: 성능 최적화 필요
// FIXME: 에지 케이스 처리
// HACK: 임시 해결책, 나중에 리팩토링 필요
// NOTE: 중요한 정보
```

---

## Git 커밋

### 커밋 메시지 형식

```
<타입>: <제목>

<본문> (선택사항)

<푸터> (선택사항)
```

### 타입

```
feat: 새로운 기능 추가
fix: 버그 수정
docs: 문서 변경
style: 코드 포맷팅 (로직 변경 없음)
refactor: 코드 리팩토링
test: 테스트 추가/수정
chore: 빌드, 설정 변경
```

### 예시

```bash
# ✅ 좋은 커밋 메시지
feat: 연차 신청 페이지 구현
fix: 슬롯 중복 신청 버그 수정
refactor: 형평성 계산 로직 개선
docs: API 문서 업데이트

# ❌ 나쁜 커밋 메시지
update
fix bug
wip
```

### 커밋 크기

```
✅ 하나의 논리적 변경만 포함
✅ 작고 자주 커밋
✅ 기능별로 분리

❌ 여러 기능을 한 번에 커밋
❌ 거대한 커밋
```

---

## Prettier 설정

**파일**: `.prettierrc.json`

```json
{
  "semi": false,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100,
  "arrowParens": "always"
}
```

---

## ESLint 규칙

**파일**: `.eslintrc.json`

```json
{
  "extends": [
    "next/core-web-vitals",
    "plugin:@typescript-eslint/recommended",
    "prettier"
  ],
  "rules": {
    "@typescript-eslint/no-explicit-any": "error",
    "@typescript-eslint/no-unused-vars": "error",
    "no-console": ["warn", { "allow": ["warn", "error"] }],
    "prefer-const": "error",
    "no-var": "error"
  }
}
```

---

## 요약 체크리스트

### 파일 작성 전 확인
- [ ] 파일명이 규칙에 맞는가?
- [ ] 타입이 명시되어 있는가?
- [ ] any 타입을 사용하지 않았는가?

### 컴포넌트 작성 시
- [ ] 명명된 내보내기를 사용하는가?
- [ ] Props 타입이 정의되어 있는가?
- [ ] key 속성이 올바른가?

### API 작성 시
- [ ] 인증 확인을 하는가?
- [ ] try-catch로 감싸져 있는가?
- [ ] 응답 형식이 일관적인가?
- [ ] 상태 코드가 적절한가?

### 커밋 전 확인
- [ ] Prettier 포맷팅을 적용했는가?
- [ ] ESLint 에러가 없는가?
- [ ] 테스트가 통과하는가?
- [ ] 커밋 메시지가 명확한가?

---

**이 컨벤션을 준수하여 일관성 있는 코드를 작성하세요!**
