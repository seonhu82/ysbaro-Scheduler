# 연세바로치과 스케줄러 - 개발 체크리스트

**파일별 상세 작업 목록**
**작성일**: 2025-10-22
**버전**: 1.0

---

## 📊 전체 진행 현황

```
총 185개 파일

Phase 1 (Week 1-2): 25개 파일 - [ ] 0/25 (0%)
Phase 2 (Week 3-7): 100개 파일 - [ ] 0/100 (0%)
Phase 3 (Week 8-10): 60개 파일 - [ ] 0/60 (0%)
```

---

## Phase 1: 기초 설정 (Week 1-2)

### Week 1: 환경 설정

#### 1. 프로젝트 초기화 (1일차)
```bash
□ Next.js 프로젝트 생성
  npx create-next-app@latest dental-scheduler --typescript --tailwind --app

□ 의존성 설치
  npm install @prisma/client prisma bcryptjs next-auth@beta
  npm install -D @types/bcryptjs

□ 개발 도구 설정
  npm install -D eslint prettier eslint-config-prettier
  npm install zustand @tanstack/react-query date-fns
  npm install lucide-react class-variance-authority clsx tailwind-merge
```

**체크포인트**:
- [ ] `npm run dev` 정상 실행
- [ ] TypeScript 에러 없음
- [ ] Tailwind CSS 정상 작동

---

#### 2. 데이터베이스 설정 (1일차)

**파일**: `prisma/schema.prisma`
```
□ Prisma 초기화
  npx prisma init

□ PostgreSQL 연결 설정
  .env.local 파일에 DATABASE_URL 설정

□ 스키마 작성 (25개 모델)
  ✓ User, Clinic
  ✓ Doctor, DoctorPattern, DoctorPatternDay
  ✓ Staff, StaffRankSettings
  ✓ Schedule, ScheduleDoctor, StaffAssignment
  ✓ ApplicationLink, SlotLimit, LeaveApplication
  ✓ ScheduleViewLink, DeploymentSettings
  ✓ Holiday
  ✓ RuleSettings, FairnessSettings, SpecialCondition
  ✓ NotificationSettings, BackupConfig, Backup
  ✓ FairnessScore
  ✓ Notification
  ✓ ActivityLog

□ 마이그레이션 실행
  npx prisma migrate dev --name init

□ Prisma Client 생성
  npx prisma generate
```

**체크포인트**:
- [ ] 마이그레이션 성공
- [ ] 데이터베이스 연결 확인
- [ ] Prisma Studio 정상 작동 (`npx prisma studio`)

**참고**: `docs/연세바로치과_기능명세서_Part3_v2.1.md` 섹션 4.1

---

#### 3. Seed 데이터 작성 (1일차)

**파일**: `prisma/seed.ts`
```typescript
□ Seed 스크립트 작성
  - Clinic 1개
  - Admin User 1개
  - Doctor 5명
  - Staff 20명 (팀장 2, 고년차 4, 중년차 6, 저년차 8)
  - Holiday 2025년 공휴일
  - RuleSettings, FairnessSettings 기본값

□ package.json에 seed 추가
  "prisma": {
    "seed": "ts-node --compiler-options {\"module\":\"CommonJS\"} prisma/seed.ts"
  }

□ Seed 실행
  npx prisma db seed
```

**체크포인트**:
- [ ] Seed 실행 성공
- [ ] Prisma Studio에서 데이터 확인

**참고**: `docs/연세바로치과_기능명세서_Part3_v2.1.md` 섹션 4.3

---

#### 4. 코어 라이브러리 (1일차)

**파일**: `src/lib/prisma.ts`
```typescript
□ Prisma 클라이언트 싱글톤 생성
  - globalThis 활용
  - Development/Production 환경 분리
```

**파일**: `src/lib/auth.ts`
```typescript
□ NextAuth 설정
  - Credentials provider
  - JWT 전략
  - 세션 콜백
```

**파일**: `src/lib/utils.ts`
```typescript
□ 유틸리티 함수
  - cn() - Tailwind 클래스 병합
  - formatDate() - 날짜 포맷
  - formatNumber() - 숫자 포맷
```

**파일**: `src/lib/date-utils.ts`
```typescript
□ 날짜 헬퍼 함수
  - getMonthDates() - 월간 날짜 배열
  - isWeekend() - 주말 여부
  - isHoliday() - 휴일 여부
  - getWeekNumber() - 주차 계산
```

**체크포인트**:
- [ ] 모든 유틸리티 함수 테스트
- [ ] TypeScript 타입 정의 완료

---

#### 5. 타입 정의 (2일차)

**파일**: `src/types/index.ts`
```typescript
□ 공통 타입 정의
  - ApiResponse<T>
  - PaginationParams
  - FilterParams
```

**파일**: `src/types/schedule.ts`
```typescript
□ 스케줄 관련 타입
  - ScheduleWithRelations
  - StaffAssignmentWithStaff
  - RequiredStaff
```

**파일**: `src/types/staff.ts`
```typescript
□ 직원 관련 타입
  - StaffWithRelations
  - StaffRankDisplay
```

**파일**: `src/types/leave.ts` ⭐
```typescript
□ 연차 관련 타입
  - LeaveApplicationWithStaff
  - SlotStatus
  - WeeklyOffCount
```

**파일**: `src/types/pattern.ts` ⭐
```typescript
□ 패턴 관련 타입
  - DoctorPatternData
  - PatternDayData
```

**파일**: `src/types/notification.ts` ⭐
```typescript
□ 알림 관련 타입
  - NotificationWithUser
  - NotificationCreateInput
```

**파일**: `src/types/api.ts`
```typescript
□ API 응답 타입
  - SuccessResponse<T>
  - ErrorResponse
  - ValidationError
```

---

### Week 2: 인증 및 기본 UI

#### 6. shadcn/ui 컴포넌트 설치 (2일차)

```bash
□ shadcn/ui 초기화
  npx shadcn-ui@latest init

□ 필요한 컴포넌트 설치 (15개)
  npx shadcn-ui@latest add button
  npx shadcn-ui@latest add input
  npx shadcn-ui@latest add dialog
  npx shadcn-ui@latest add select
  npx shadcn-ui@latest add checkbox
  npx shadcn-ui@latest add calendar
  npx shadcn-ui@latest add dropdown-menu
  npx shadcn-ui@latest add tabs
  npx shadcn-ui@latest add card
  npx shadcn-ui@latest add badge
  npx shadcn-ui@latest add toast
  npx shadcn-ui@latest add table
  npx shadcn-ui@latest add progress
  npx shadcn-ui@latest add skeleton
  npx shadcn-ui@latest add alert
```

**체크포인트**:
- [ ] 모든 컴포넌트 정상 설치
- [ ] Storybook에서 확인 (선택사항)

---

#### 7. 인증 시스템 (3일차)

**파일**: `src/app/api/auth/[...nextauth]/route.ts`
```typescript
□ NextAuth 라우트 핸들러
  - 로그인 로직
  - 세션 관리
  - JWT 발급

□ 테스트 계정 로그인 확인
  admin@dental.com / admin123!
```

**파일**: `src/app/(auth)/login/page.tsx`
```typescript
□ 로그인 페이지
  - 이메일/비밀번호 입력
  - 로그인 버튼
  - 에러 메시지 표시
  - 로그인 성공 시 /calendar로 리다이렉트
```

**파일**: `src/app/(auth)/layout.tsx`
```typescript
□ 인증 레이아웃
  - 중앙 정렬 카드
  - 로고 표시
  - 반응형 디자인
```

**체크포인트**:
- [ ] 로그인 성공
- [ ] 세션 유지
- [ ] 로그아웃 동작

**참고**: `docs/연세바로치과_기능명세서_Part3_v2.1.md` 섹션 8.1-8.2

---

#### 8. 기본 레이아웃 (3일차)

**파일**: `src/app/layout.tsx`
```typescript
□ 루트 레이아웃
  - HTML/Body 태그
  - 폰트 설정
  - 메타데이터
  - Providers (React Query, Auth)
```

**파일**: `src/app/(dashboard)/layout.tsx`
```typescript
□ 대시보드 레이아웃
  - 헤더
  - 사이드바
  - 메인 콘텐츠 영역
  - 인증 확인
```

**파일**: `src/components/layout/Header.tsx`
```typescript
□ 헤더 컴포넌트
  - 로고
  - 알림 벨 (NotificationBell)
  - 사용자 메뉴
  - 로그아웃 버튼
```

**파일**: `src/components/layout/Sidebar.tsx`
```typescript
□ 사이드바 컴포넌트
  - 달력 메뉴
  - 연차관리 메뉴 ⭐
  - 통계 메뉴
  - 알림 메뉴 ⭐
  - 설정 메뉴
  - 로그 메뉴
```

**파일**: `src/components/layout/Footer.tsx`
```typescript
□ 푸터 컴포넌트
  - 저작권 표시
  - 버전 정보
```

**체크포인트**:
- [ ] 레이아웃 정상 렌더링
- [ ] 반응형 동작 확인
- [ ] 메뉴 네비게이션 동작

---

#### 9. 달력 기본 구조 (4일차)

**파일**: `src/app/(dashboard)/calendar/page.tsx`
```typescript
□ 달력 메인 페이지
  - 월 선택기
  - 달력 그리드
  - 스케줄 불러오기
```

**파일**: `src/components/calendar/CalendarView.tsx`
```typescript
□ 달력 뷰 컴포넌트
  - 7x6 그리드 (주x일)
  - 각 셀에 스케줄 표시
```

**파일**: `src/components/calendar/CalendarGrid.tsx`
```typescript
□ 달력 그리드
  - 주 단위 행
  - 셀 렌더링
```

**파일**: `src/components/calendar/CalendarCell.tsx`
```typescript
□ 달력 셀
  - 날짜 표시
  - 원장 목록
  - 직원 수 표시
  - 클릭 이벤트
```

**파일**: `src/components/calendar/MonthNavigator.tsx`
```typescript
□ 월 네비게이터
  - 이전/다음 월 버튼
  - 현재 월 표시
  - 오늘로 이동 버튼
```

**체크포인트**:
- [ ] 달력 정상 렌더링
- [ ] 월 이동 동작
- [ ] 셀 클릭 이벤트

---

## Phase 2: 핵심 기능 (Week 3-7)

### Week 3: 원장 패턴 관리

#### 10. 패턴 UI (5일차)

**파일**: `src/app/(dashboard)/settings/doctors/pattern/page.tsx`
```typescript
□ 패턴 설정 페이지
  - 요일별 탭
  - 패턴 편집 폼
  - 저장 버튼
```

**파일**: `src/components/pattern/DayPatternEditor.tsx` ⭐
```typescript
□ 요일 패턴 편집기
  - 원장 체크박스 (5명)
  - 야간 진료 체크박스
  - 실시간 미리보기
```

**파일**: `src/components/pattern/PatternPreview.tsx` ⭐
```typescript
□ 패턴 미리보기
  - 선택된 원장 표시
  - 필요 인원 계산 표시
```

**파일**: `src/components/pattern/PatternApplyButton.tsx` ⭐
```typescript
□ 패턴 적용 버튼
  - 월 선택 모달
  - 적용 확인
  - 진행률 표시
```

**체크포인트**:
- [ ] 패턴 편집 동작
- [ ] 미리보기 실시간 업데이트
- [ ] 저장 성공

---

#### 11. 패턴 API (5일차)

**파일**: `src/app/api/doctor-pattern/route.ts` ⭐
```typescript
□ GET - 패턴 조회
□ POST - 패턴 생성/수정
```

**파일**: `src/app/api/doctor-pattern/[id]/route.ts` ⭐
```typescript
□ PUT - 특정 패턴 수정
□ DELETE - 패턴 삭제
```

**파일**: `src/app/api/doctor-pattern/apply/route.ts` ⭐
```typescript
□ POST - 월간 패턴 적용
  - 트랜잭션 처리
  - 기존 스케줄 확인
  - 일괄 생성
```

**파일**: `src/lib/services/pattern-service.ts` ⭐
```typescript
□ 패턴 서비스 로직
  - applyPatternToMonth()
  - validatePattern()
  - calculateRequiredStaff()
```

**체크포인트**:
- [ ] API 테스트 (Postman/Thunder Client)
- [ ] 에러 핸들링 확인
- [ ] 트랜잭션 롤백 확인

**참고**: `docs/연세바로치과_기능명세서_Part2_v2.1.md` 섹션 3.3.1

---

### Week 4: 연차 신청 시스템

#### 12. 신청 링크 관리 API (6일차)

**파일**: `src/app/api/leave-management/period/route.ts` ⭐
```typescript
□ POST - 신청 기간 생성
  - ApplicationLink 생성
  - SlotLimit 일괄 생성
  - 토큰 발급
```

**파일**: `src/app/api/leave-management/period/[id]/route.ts` ⭐
```typescript
□ PUT - 기간 수정
□ DELETE - 기간 삭제 (신청 없을 때만)
```

**파일**: `src/app/api/leave-management/period/[id]/confirm/route.ts` ⭐
```typescript
□ POST - 연차 확정 (트랜잭션)
  - LeaveApplication 상태 변경
  - Staff.annualLeaveUsed 증가
  - 스케줄에서 직원 제거
```

**체크포인트**:
- [ ] 링크 생성 성공
- [ ] 토큰 고유성 확인
- [ ] 확정 트랜잭션 정상 동작

**참고**: `docs/연세바로치과_기능명세서_Part2_v2.1.md` 섹션 3.3.2

---

#### 13. 직원 신청 페이지 (7-8일차)

**파일**: `src/app/(public)/leave-apply/[token]/page.tsx` ⭐
```typescript
□ 신청 메인 페이지
  - 토큰 검증
  - 인증 폼 (생년월일/PIN)
  - 신청 폼
  - 내 신청 내역
```

**파일**: `src/components/public-apply/DateSelector.tsx` ⭐
```typescript
□ 날짜 선택기
  - 신청 가능한 날짜만 활성화
  - 슬롯 현황 표시
  - 휴일 표시 ⭐
```

**파일**: `src/components/public-apply/TypeSelector.tsx` ⭐
```typescript
□ 유형 선택 (연차/오프)
  - 라디오 버튼
  - 오프 제한 안내
```

**파일**: `src/components/public-apply/RealTimeStatus.tsx` ⭐
```typescript
□ 실시간 슬롯 현황 (SSE)
  - 3초마다 업데이트
  - 슬롯 점유율 표시
  - 주간 오프 현황
```

**파일**: `src/components/public-apply/ApplicationForm.tsx` ⭐
```typescript
□ 신청 폼
  - 날짜/유형 선택
  - 확인 다이얼로그
  - 신청 API 호출
```

**파일**: `src/components/public-apply/ConfirmationModal.tsx` ⭐
```typescript
□ 신청 확인 모달
  - 날짜/유형 표시
  - 주간 오프 경고
  - 확인/취소 버튼
```

**체크포인트**:
- [ ] 토큰 인증 동작
- [ ] 실시간 업데이트 확인
- [ ] 신청 성공
- [ ] 휴일 신청 차단 확인

**참고**: `docs/연세바로치과_기능명세서_Part2_v2.1.md` 섹션 3.3.3

---

#### 14. 신청 API (8-9일차)

**파일**: `src/app/api/leave-apply/[token]/route.ts` ⭐
```typescript
□ GET - 신청 페이지 데이터
  - 링크 정보
  - 직원 목록
  - 슬롯 제한
```

**파일**: `src/app/api/leave-apply/[token]/submit/route.ts` ⭐⭐⭐
**중요도**: 🔴 최고

```typescript
□ POST - 연차/오프 신청
  1. 임시 토큰 검증
  2. 신청 링크 확인
  3. 날짜 유효성 확인
  4. ⭐ 휴일 체크 (일요일 또는 공휴일)
  5. ⭐ 오프 주간 제한 체크 (주 2일)
  6. 슬롯 제한 확인
  7. 중복 신청 확인
  8. 트랜잭션으로 LeaveApplication 생성
  9. 알림 생성
  10. 활동 로그

□ 동시성 제어
  - Prisma 트랜잭션
  - Optimistic locking
  - 슬롯 재확인
```

**파일**: `src/app/api/leave-apply/[token]/status/route.ts` ⭐
```typescript
□ GET - 슬롯 현황 조회
  - 날짜별 슬롯 상태
  - ⭐ 휴일 정보 포함
  - 주간 오프 개수
  - 직원 연차 정보
```

**파일**: `src/lib/services/leave-service.ts` ⭐⭐
```typescript
□ 연차 서비스 로직
  - checkSlotAvailability()
  - checkWeeklyOffLimit() ⭐
  - isHoliday() ⭐
  - createLeaveApplication()
```

**체크포인트**:
- [ ] 동시 신청 시 슬롯 제한 동작
- [ ] 주 2일 오프 제한 동작
- [ ] 휴일 신청 차단
- [ ] 트랜잭션 롤백 확인

**참고**: `docs/연세바로치과_기능명세서_Part2_v2.1.md` 섹션 3.3.3 (line 431-603)

---

### Week 5: 연차 관리 대시보드

#### 15. 관리 대시보드 UI (10-11일차)

**파일**: `src/app/(dashboard)/leave-management/page.tsx` ⭐
```typescript
□ 연차관리 메인 페이지
  - 월 선택
  - 뷰 전환 탭 (달력/목록/직원별)
  - 통계 요약
```

**파일**: `src/components/leave-management/Dashboard.tsx` ⭐
```typescript
□ 대시보드 컴포넌트
  - 통계 카드 (총 신청, 확정, 대기, 취소)
  - 신청 링크 생성 버튼
  - 뷰 전환 탭
```

**파일**: `src/components/leave-management/CalendarView.tsx` ⭐
```typescript
□ 달력뷰
  - 월간 달력
  - 각 날짜별 신청자 표시
  - 슬롯 마감 표시
  - 날짜 클릭 시 상세 팝업
```

**파일**: `src/components/leave-management/ListView.tsx` ⭐
```typescript
□ 목록뷰
  - 필터 (날짜, 직원, 상태)
  - 정렬
  - 체크박스 선택
  - 일괄 확정/취소 버튼
```

**파일**: `src/components/leave-management/StaffView.tsx` ⭐
```typescript
□ 직원별뷰
  - 직원 목록
  - 연차 사용 현황
  - 신청 내역 타임라인
```

**파일**: `src/components/leave-management/PeriodSetup.tsx` ⭐
```typescript
□ 신청 기간 설정
  - 연/월 선택
  - 신청 기간 설정
  - 슬롯 제한 설정
  - 링크 생성
```

**체크포인트**:
- [ ] 모든 뷰 정상 렌더링
- [ ] 필터/정렬 동작
- [ ] 체크박스 선택 동작

**참고**: `docs/연세바로치과_기능명세서_Part2_v2.1.md` 섹션 3.3.4

---

#### 16. 관리 API (11일차)

**파일**: `src/app/api/leave-management/route.ts` ⭐
```typescript
□ GET - 전체 신청 현황
  - 필터/정렬 파라미터
  - 페이지네이션
```

**파일**: `src/app/api/leave-management/calendar-view/route.ts` ⭐
```typescript
□ GET - 달력뷰 데이터
  - 날짜별 신청 집계
```

**파일**: `src/app/api/leave-management/list-view/route.ts` ⭐
```typescript
□ GET - 목록뷰 데이터
  - 상세 정보 포함
```

**파일**: `src/app/api/leave-management/staff-view/route.ts` ⭐
```typescript
□ GET - 직원별뷰 데이터
  - 직원별 집계
```

**파일**: `src/app/api/leave-management/[id]/route.ts` ⭐
```typescript
□ PUT - 신청 수정
□ DELETE - 신청 삭제 (확정 전만)
```

---

### Week 6: 자동 배치 시스템

#### 17. 배치 알고리즘 (12-14일차)

**파일**: `src/lib/algorithms/monthly-assign.ts` ⭐⭐⭐
**중요도**: 🔴 최고

```typescript
□ 월간 배치 알고리즘
  1. 데이터 수집
     - 대상 날짜
     - 활성 직원
     - 연차 신청
     - 제약 조건

  2. 배치 방식 결정
     - 완전 재배치 vs 스마트 배치

  3. 완전 재배치
     - 기존 배치 초기화
     - 각 날짜별 배치
     - 비율 기반 선발
     - 형평성 고려

  4. 스마트 배치
     - 문제 있는 날짜만 수정
     - 기존 배치 최대한 유지

  5. 검증 및 저장
     - 필수 인원 충족 확인
     - 제약 조건 위반 확인
     - DB 저장
     - 형평성 점수 재계산
```

**파일**: `src/lib/algorithms/weekly-assign.ts` ⭐⭐
```typescript
□ 주간 배치 알고리즘
  - 특정 주만 배치
  - 월간 배치와 유사하지만 범위 제한
```

**파일**: `src/lib/algorithms/fairness-calculator.ts` ⭐⭐
```typescript
□ 형평성 계산
  - 야간 근무 평균/편차
  - 주말 근무 평균/편차
  - 가중 평균 점수
  - 등급 산정
```

**파일**: `src/lib/algorithms/validation.ts`
```typescript
□ 검증 로직
  - validateAssignment()
  - checkConstraints()
  - checkRequiredStaff()
```

**체크포인트**:
- [ ] 월간 배치 성공
- [ ] 형평성 점수 정상 계산
- [ ] 제약 조건 위반 감지

**참고**: `docs/연세바로치과_기능명세서_Part2_v2.1.md` 섹션 3.4

---

#### 18. 배치 UI (14-15일차)

**파일**: `src/components/schedule/AutoAssignModal.tsx` (수정)
```typescript
□ 자동 배치 모달
  - 배치 범위 선택 (월간/주간/일별) ⭐
  - 배치 방식 선택 (완전/스마트) ⭐
  - 등급별 비율 설정 ⭐
  - 시작 버튼
```

**파일**: `src/components/schedule/ValidationResult.tsx`
```typescript
□ 검증 결과 표시
  - 성공/실패 개수
  - 경고 목록
  - 형평성 점수
```

**파일**: `src/components/schedule/FairnessChart.tsx`
```typescript
□ 형평성 차트
  - 막대 그래프
  - 히트맵
  - 시간 경과 추이
```

---

#### 19. 배치 API (15일차)

**파일**: `src/app/api/auto-assign/monthly/route.ts` ⭐⭐
```typescript
□ POST - 월간 배치
  - 배치 설정 파라미터
  - 배치 엔진 호출
  - 진행률 스트리밍 (선택사항)
  - 결과 반환
```

**파일**: `src/app/api/auto-assign/weekly/route.ts` ⭐
```typescript
□ POST - 주간 배치
```

**파일**: `src/app/api/auto-assign/daily/route.ts`
```typescript
□ POST - 일별 배치
```

**체크포인트**:
- [ ] 배치 API 응답 < 30초
- [ ] 진행률 표시 동작 (선택사항)
- [ ] 배치 결과 정확성

**참고**: `docs/연세바로치과_기능명세서_Part2_v2.1.md` 섹션 3.4.2

---

### Week 7: 알림 시스템

#### 20. 알림 인프라 (16-17일차)

**파일**: `src/app/api/notifications/sse/route.ts` ⭐⭐
**중요도**: 🔴 높음

```typescript
□ Server-Sent Events 구현
  - ReadableStream 생성
  - 3초마다 새 알림 확인
  - JSON 인코딩
  - 클라이언트 연결 관리
```

**파일**: `src/components/notifications/NotificationPanel.tsx` ⭐
```typescript
□ 알림 패널
  - SSE 연결
  - 실시간 업데이트
  - 알림 목록
  - 브라우저 알림 권한 요청
```

**파일**: `src/components/layout/NotificationBell.tsx` ⭐
```typescript
□ 알림 벨 아이콘
  - 읽지 않은 알림 배지
  - 클릭 시 패널 열기
```

**파일**: `src/components/notifications/NotificationItem.tsx` ⭐
```typescript
□ 알림 아이템
  - 타입별 아이콘
  - 제목/메시지
  - 시간 표시
  - 읽음 처리
  - 액션 버튼
```

**체크포인트**:
- [ ] SSE 연결 성공
- [ ] 실시간 알림 수신
- [ ] 브라우저 알림 동작

**참고**: `docs/연세바로치과_기능명세서_Part2_v2.1.md` 섹션 3.7

---

#### 21. 알림 API (17일차)

**파일**: `src/app/api/notifications/route.ts` ⭐
```typescript
□ GET - 알림 목록
  - 페이지네이션
  - 필터 (읽음/안 읽음)
```

**파일**: `src/app/api/notifications/unread/route.ts` ⭐
```typescript
□ GET - 읽지 않은 알림
```

**파일**: `src/app/api/notifications/[id]/read/route.ts` ⭐
```typescript
□ PUT - 알림 읽음 처리
```

**파일**: `src/app/api/notifications/[id]/route.ts` ⭐
```typescript
□ DELETE - 알림 삭제
```

**파일**: `src/app/api/notifications/read-all/route.ts` ⭐
```typescript
□ PUT - 모두 읽음 처리
```

**파일**: `src/lib/services/notification-service.ts` ⭐
```typescript
□ 알림 서비스
  - createNotification()
  - sendBrowserNotification()
  - markAsRead()
```

---

## Phase 3: 완성 및 테스트 (Week 8-10)

### Week 8: 통계 및 설정

#### 22. 통계 페이지 (18일차)

**파일**: `src/app/(dashboard)/statistics/page.tsx`
```typescript
□ 통계 메인 페이지
  - 기간 선택
  - 차트 표시
  - Excel 다운로드
```

**파일**: `src/components/statistics/Overview.tsx`
```typescript
□ 개요 통계
  - 총 근무일
  - 평균 야간 근무
  - 평균 주말 근무
```

**파일**: `src/components/statistics/StaffWorkload.tsx`
```typescript
□ 직원별 근무 부하
  - 막대 차트
  - 순위 표시
```

**파일**: `src/components/statistics/FairnessReport.tsx`
```typescript
□ 형평성 리포트
  - 등급별 분포
  - 시간 추이
```

**파일**: `src/components/statistics/MonthlyTrend.tsx`
```typescript
□ 월별 추이
  - 선 그래프
  - 비교 분석
```

---

#### 23. 설정 페이지 (19-20일차)

**파일**: `src/app/(dashboard)/settings/page.tsx`
```typescript
□ 설정 메인
  - 탭 네비게이션
```

**파일**: `src/app/(dashboard)/settings/staff/page.tsx`
```typescript
□ 직원 관리
  - 목록
  - 추가/수정/삭제
  - 일괄 등록
```

**파일**: `src/app/(dashboard)/settings/doctors/page.tsx`
```typescript
□ 원장 관리
  - 목록
  - 추가/수정
```

**파일**: `src/app/(dashboard)/settings/rules/page.tsx`
```typescript
□ 근무 규칙
  - 주 근무일
  - 필수 인원
  - 연속 근무 제한
```

**파일**: `src/app/(dashboard)/settings/holidays/page.tsx`
```typescript
□ 휴업일 관리
  - 공휴일 목록
  - 추가/삭제
```

**파일**: `src/app/(dashboard)/settings/backup/page.tsx`
```typescript
□ 백업/복원
  - 수동 백업
  - 백업 목록
  - 복원
  - 자동 백업 설정
```

---

### Week 9: 추가 기능

#### 24. 스케줄 배포 (21일차)

**파일**: `src/app/api/deploy/schedule-link/route.ts` ⭐
```typescript
□ POST - 스케줄 확인 링크 생성
  - ScheduleViewLink 생성
  - 토큰 발급
```

**파일**: `src/app/(public)/schedule-view/[token]/page.tsx` ⭐
```typescript
□ 스케줄 조회 페이지
  - 인증
  - 개인/전체/원장 스케줄 조회
  - 다운로드
```

---

#### 25. Excel/PDF 생성 (22일차)

**파일**: `src/app/api/export/excel/route.ts`
```typescript
□ POST - Excel 생성
  - exceljs 사용
  - 스타일링
  - 다운로드 링크 반환
```

**파일**: `src/app/api/export/pdf/route.ts`
```typescript
□ POST - PDF 생성
  - puppeteer 또는 react-pdf
  - 인쇄용 레이아웃
```

**파일**: `src/lib/services/export-service.ts`
```typescript
□ 내보내기 서비스
  - generateExcel()
  - generatePDF()
```

---

### Week 10: 테스트 및 배포

#### 26. 테스트 작성 (23-24일차)

**파일**: `__tests__/unit/algorithms.test.ts`
```typescript
□ 알고리즘 단위 테스트
  - calculateRequiredStaff
  - calculateFairnessScore
  - selectByRatio
```

**파일**: `__tests__/unit/services.test.ts`
```typescript
□ 서비스 단위 테스트
  - leave-service
  - pattern-service
  - notification-service
```

**파일**: `__tests__/integration/api.test.ts`
```typescript
□ API 통합 테스트
  - 스케줄 API
  - 연차 신청 API
  - 자동 배치 API
```

**파일**: `__tests__/integration/leave-application.test.ts`
```typescript
□ 연차 신청 시나리오 테스트
  - 슬롯 제한
  - 주간 오프 제한
  - 동시성
```

---

#### 27. 배포 준비 (25일차)

**파일**: `vercel.json`
```json
□ Vercel 설정
  - 빌드 커맨드
  - 환경 변수
  - 리전 설정
```

**파일**: `.env.example`
```
□ 환경 변수 예시
  - DATABASE_URL
  - NEXTAUTH_SECRET
  - NEXTAUTH_URL
```

**파일**: `README.md`
```markdown
□ 프로젝트 README
  - 소개
  - 설치 방법
  - 환경 변수 설정
  - 실행 방법
```

```bash
□ 배포
  vercel --prod
```

---

## 📊 진행 추적

### 파일 완성도 체크

```
Phase 1: [ ] 0/25 (0%)
  Week 1: [ ] 0/15
  Week 2: [ ] 0/10

Phase 2: [ ] 0/100 (0%)
  Week 3: [ ] 0/15
  Week 4: [ ] 0/25
  Week 5: [ ] 0/20
  Week 6: [ ] 0/25
  Week 7: [ ] 0/15

Phase 3: [ ] 0/60 (0%)
  Week 8: [ ] 0/20
  Week 9: [ ] 0/20
  Week 10: [ ] 0/20

전체: [ ] 0/185 (0%)
```

---

## 🎯 우선순위 파일 (반드시 완성)

### 🔴 최우선 (20개)
```
□ prisma/schema.prisma
□ src/app/api/leave-apply/[token]/submit/route.ts
□ src/lib/algorithms/monthly-assign.ts
□ src/lib/algorithms/fairness-calculator.ts
□ src/app/api/notifications/sse/route.ts
□ src/components/public-apply/RealTimeStatus.tsx
□ src/lib/services/leave-service.ts
□ src/app/api/auto-assign/monthly/route.ts
□ src/app/api/leave-management/period/[id]/confirm/route.ts
□ src/components/schedule/AutoAssignModal.tsx
□ src/app/(public)/leave-apply/[token]/page.tsx
□ src/app/(dashboard)/leave-management/page.tsx
□ src/lib/hooks/use-leave-management.ts
□ src/lib/algorithms/validation.ts
□ src/app/api/leave-apply/[token]/status/route.ts
□ src/components/leave-management/Dashboard.tsx
□ src/lib/services/notification-service.ts
□ src/app/api/doctor-pattern/apply/route.ts
□ src/components/calendar/PatternApplyButton.tsx
□ src/lib/algorithms/weekly-assign.ts
```

---

**다음 단계**: `.claude/02_코딩_컨벤션.md` 확인
